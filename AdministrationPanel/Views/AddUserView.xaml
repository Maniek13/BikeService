<Page
    x:Class="ToDoApp.Views.AddUserView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ToDoApp.ModelViews"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
    <Page.DataContext>
        <local:UserViewModel/>
    </Page.DataContext>
    <Grid>
        <StackPanel Width="300" HorizontalAlignment="Center" VerticalAlignment="Center" Canvas.ZIndex="1000">
            <TextBox 
            x:Name="loginInput"
                        Header="Wpisz login:" PlaceholderText="login" Canvas.ZIndex="100" Width="NaN" Height="60"/>
            <TextBox 
            x:Name="paswordInput"
                        Header="Wpisz hasło:" PlaceholderText="hasło" Canvas.ZIndex="100" Width="NaN" Height="60" Margin="0,10,0,0"/>
            <Button Click="Add_Click" HorizontalAlignment="Stretch" Canvas.ZIndex="100" Margin="0,10,0,0">
                Dodaj
            </Button>
            <TextBlock Text="{x:Bind Path=viewModel.User.Login, Mode=TwoWay}" Margin="0,5,0,0"></TextBlock>
            <TextBlock Text="{x:Bind Path=viewModel.User.Password, Mode=TwoWay}" Margin="0,5,0,0"></TextBlock>
        </StackPanel>
    </Grid>
</Page>
