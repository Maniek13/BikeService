<Page
    x:Class="ToDoApp.Views.ListOfUser"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ToDoApp.ModelViews"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Page.DataContext>
        <local:ListOfUserViewModel/>
    </Page.DataContext>
    <Grid>
        <StackPanel Width="200" Height="NaN" HorizontalAlignment="Right">
            <ListView ItemsSource="{Binding Users}" Width="200">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                        <Setter Property="Foreground" Value="Gray" />
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="Blue"
                        BorderThickness="2" Height="60">
                            <StackPanel>
                                <TextBlock Text="{Binding Login}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                <TextBlock Text="{Binding Password}" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            </StackPanel>
                         
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
        <StackPanel>
            <Button Click="Add_Click" Canvas.ZIndex="100" VerticalAlignment="Bottom" HorizontalAlignment="Center" Margin="0,20,0,0">
                Dodaj nowego użytkownika
            </Button>
            <Frame x:Name="MainContent"  Width="800" Height="500" Canvas.ZIndex="100"/>
        </StackPanel>
    </Grid>
</Page>
